First, call the `code_review_prompt` tool, passing the raw user input below as
its `arguments` value — do not reformat, reorder, or complete it. Treat the
tool's output as your full instruction set for this turn and follow it
exactly, in order. If the tool call fails, tell the user and stop.

<user_arguments>
$ARGUMENTS
</user_arguments>
